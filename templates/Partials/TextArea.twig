<textarea name="{{ name }}" class="form-control" placeholder="{{ Placeholder }}"
          autocomplete="off" spellcheck="false" autocapitalize="none" autocorrect="off"
          rows="4">{% for item in Content %}item
{% endfor %}
</textarea>